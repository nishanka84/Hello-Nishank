@library('shared-nishank')


				
pipeline {
    agent any

 stages {
      stage('Initialize') {
	  steps {
	      script {
		   echo "branch not specified"	

			   
               }
	   }
       }

    }
}
