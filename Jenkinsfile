
@Library('shared-nishank') _

pipeline {
    agent none
    stages {
        stage ('Example') {
            steps {
                // log.info 'Starting' 
                script { 
                    echo "branch not specified"
                }
            }
        }
    }
}
